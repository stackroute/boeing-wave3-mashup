server.port=8092
spring.application.name=api-gateway
eureka.client.serviceUrl.defaultZone=http://localhost:8093/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
spring.application.profiles.active= development
spring.application.cloud.config.uri= http://localhost:8032

server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=/DB/resources/keystore.p12
# The password used to generate the certificate
server.ssl.key-store-password=parul093
# The alias mapped to the certificate
server.ssl.key-alias=tomcat
